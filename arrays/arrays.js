//Para las primeras 5 preguntas tendrás que crear un array con las especificaciones que se te pida para cada caso.

//Crea un array de 5 elementos:
//El elemento del indice 4 tendra el string "Thom", el 2 "Gonza" y los otros tres tendrán el mismo string: "Hello"
let arrayString = null;

//Crea un array de 5 elementos:
//El elemento del indice 1 tendra el numero 8, el indice 3 = 34, en los otros indices puedes colocar el numero que gustes
let arrayNumero = null;

//Crea un array de 3 elementos:
//El elemento del indice 2 tendra el booleano true, el indice 0 = true y el indice 1 = false
let arrayBoleanos = null;

//Crea un array de 5 elementos:
//El elemento del indice 0 tendra el arrayNumero, el indice 1 = arrayBoleanos, el indice 2 = arrayString, 
//el indice 3 sera igual a un array que tenga 2 elementos el elemento del indice 0 sera el largo del arrayNumero y el indice 1 = "hola"
// el indice 4 sera igual a un array que contenga un elemento que a su vez sera un array que tendra el valor del indice 2 de arrayNumero
//en este ultimo elemento intenta no poner el valor directamente(si no lo puedes hacer así resuelvelo como gustes para pasar el test)
let arrayDeArray = null;


//Crea un array de 3 elementos:
//El elemento del indice 1 tendra el numero 141, el indice 0 = "34" y el indice 2 tendra un false
let arrayCombinado = null;

//Para los siguentes ejercicios deveras colocar tu codigo dentro de las funciones y retornar tu respuesta 
//con la palabra return

function metodoMapear(array) {
    //Recibiras un array que contiene numeros,  deberas sumarle 2 a cada elemento
    //devuelve el array modificado recuerda que tienes que utilizar el "return"
    //Tu codigo:
    return null;
}

function metodoFiltrar(array) {
    //Recibiras un array que contendra un lista de productos tienes que filtrar solo los productos y devolcer solo los 
    //"banana", "orange", "lemon" retorna el nuevo array
    //Tu codigo:
    return null;
}

function metodoAgregar(array) {
    //Ingresa al array que se te pasa el numero 50 en la ultima posición
    return null;
}
function metodoAgregarF(array) {
    //Ingresa al array que se te pasa el numero 50 en la primera posición
    return null;
}

function metodoEliminarP(array) {
    //Elimina el primer elemento del array que se te pasó y devuelve el elemento eliminado
    return null
}
function metodoEliminarF(array) {
    //Elimina el último elemento del array que se te pasó y devuelve el elemento eliminado
    return null;
}
function metodoEncontrar(array) {
    //Encuentra en el array que se te pasa el valor numerico 34 y devuelvelo
    return null;
}

function metodoOrdenarA(array) {
    //Ordena alfabeticamente el array que se te pasa y devuelve el array ordenado
    return null;
}
function metodoOrdenar_maM(array) {
    //Ordena el array de numeros que se te pasa del numero mas pequeño al mas grande
    //devuelve el array ya ordenado
    return null;
}

function metodoOrdenar_Mam(array) {
    //Ordena el array de numeros que se te pasa del numero mas grande al mas pequeño
    //devuelve el array ya ordenado
    return null;
}

function metodoUnirArray(array1, array2) {
    //Une los dos array que se te pasaron y devuelve el array nuevo
    return null;
}

function metodoTextoUnir(array) {
    //Une todos los elementos en una cadena de texto separados por giones "-"
    return null;
}


module.exports = {
    arrayString,
    arrayNumero,
    arrayBoleanos,
    arrayDeArray,
    arrayCombinado,
    metodoMapear,
    metodoFiltrar,
    metodoAgregar,
    metodoAgregarF,
    metodoEncontrar,
    metodoEliminarF,
    metodoEliminarP,
    metodoOrdenarA,
    metodoOrdenar_Mam,
    metodoOrdenar_maM,
    metodoTextoUnir,
    metodoUnirArray
}











